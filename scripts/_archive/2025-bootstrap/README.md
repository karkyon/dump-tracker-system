# 2025年9月 bootstrap期の一回限り修正スクリプト（アーカイブ）

このディレクトリのスクリプトは、プロジェクト初期構築期（2025-09時点）に
一度だけ使われた修正・セットアップスクリプトです。現行のプロジェクト運用
（`schema.camel.prisma`が正、`prisma db pull`でスキーマを上書きしない等）
とは前提が異なるため、誤って再実行しないようscripts/直下から隔離しています。

**`model_generator.sh` は特に注意**: `npx prisma db pull` を実行するため、
現行のcamelCase命名規則を破壊する可能性があります。実行しないでください。
